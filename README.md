# android-ui-analyser (`aua`)

Fast, configurable CLI that gives an AI agent structured "what's on screen and where"
for Android UI testing — hierarchy-first, with pluggable vision/OCR/grounding fallbacks.

> Full README is generated as part of the build (install, quickstart, CLAUDE.md
> snippet, provider/license matrix). See `PRD.md` and `docs/` meanwhile.
