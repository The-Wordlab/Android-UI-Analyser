# Third-party transport

The companion downloads the pinned `tun2proxy` Android native libraries from
https://github.com/tun2proxy/tun2proxy during its first build.

`tun2proxy` is Copyright its contributors and distributed under the MIT License.
The downloaded archive is verified against the SHA-256 value declared in `companion.py`.
